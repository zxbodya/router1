{
  "name": "router1",
  "version": "0.1.4",
  "description": "Complete routing solution for isomophic reactive applications",
  "main": "lib/index.js",
  "jsnext:main": "src/index.js",
  "scripts": {
    "lint": "eslint ./src/",
    "cover": "rimraf coverage && istanbul cover -x \"*.test.js\" ./node_modules/jasmine/bin/jasmine.js && npm run cover_remapping",
    "cover_remapping": "remap-istanbul -i coverage/coverage.json -o coverage/coverage-remapped.json && remap-istanbul -i coverage/coverage.json -o coverage/coverage-remapped.lcov -t lcovonly && remap-istanbul -i coverage/coverage.json -o coverage/coverage-remapped -t html",
    "test": "jasmine",
    "coveralls": "cat ./coverage/*/coverage-remapped.lcov | ./node_modules/.bin/coveralls",
    "build": "rimraf lib && babel --presets es2015 --plugins transform-runtime src --out-dir lib --source-maps",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/zxbodya/router1.git"
  },
  "keywords": [
    "router",
    "rx",
    "isomorphic",
    "universal",
    "reactive"
  ],
  "author": "Bogdan Savluk <savluk.bogdan@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zxbodya/router1/issues"
  },
  "engines": {
    "node": ">=0.12.0"
  },
  "homepage": "https://github.com/zxbodya/router1#readme",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-core": "^6.4.5",
    "babel-plugin-transform-runtime": "^6.4.3",
    "babel-preset-es2015": "^6.3.13",
    "codecov.io": "^0.1.6",
    "coveralls": "^2.11.4",
    "eslint": "^1.9.0",
    "eslint-config-airbnb": "^1.0.0",
    "eslint-plugin-react": "^3.8.0",
    "istanbul": "^0.4.1",
    "jasmine": "^2.4.1",
    "jasmine-core": "^2.4.1",
    "remap-istanbul": "^0.5.1",
    "rimraf": "^2.4.4",
    "rx": "^4.0.0"
  },
  "dependencies": {
    "babel-runtime": "^6.3.19"
  },
  "peerDependencies": {
    "rx": "*"
  }
}
